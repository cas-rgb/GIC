Drop structured IEC election extracts here for import.

Expected files:

- `province_election_results.csv`
- `municipality_election_results.csv`
- `ward_election_results.csv`

Recommended columns:

- province results:
  - `province_name,election_year,election_type,party_name,votes,vote_share,turnout,winner_flag,source_name,source_url`
- municipality results:
  - `province_name,municipality_name,election_year,election_type,party_name,votes,vote_share,turnout,winner_flag,source_name,source_url`
- ward results:
  - `province_name,municipality_name,ward_name,election_year,election_type,party_name,candidate_name,votes,vote_share,turnout,winner_flag,source_name,source_url`
